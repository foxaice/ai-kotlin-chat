version: '3.8'

services:
  mcp-chain:
    build: .
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID}
      - TODOIST_API_KEY=${TODOIST_API_KEY}
    restart: unless-stopped
    container_name: mcp-chain-demo
